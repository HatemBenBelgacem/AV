@model AV.Models.Position

@{
    ViewData["Title"] = "Produkt hinzufügen";
}

<h1>Produkt hinzufügen</h1>

<div>
    <h4>Auftrag</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Auftrag.Beschreibung)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Auftrag.Beschreibung)
        </dd>
    </dl>
       <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
             <div class="form-group">
                <label asp-for="ProduktId" class="control-label">Adresse wählen</label>
                <select asp-for="ProduktId" class="form-control" asp-items="@ViewBag.Produkte"></select>
                <span asp-validation-for="ProduktId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-outline-secondary" />
            </div>
        </form>
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>


